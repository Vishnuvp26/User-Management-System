<% include('../layouts/header.ejs') %>

<h3> Welcome, <%= user.name %> </h3>

<table>
    <tr>
        <td>
            <img src="userImages/<%= user.image %>" width="120px" height="100px" alt="<%= user.image %>">
        </td>
    </tr>
    <tr>
        <td>Email</td>
        <td><%= user.email %></td>
    </tr>
    <tr>
        <td>Mobile</td>
        <td><%= user.mobile %></td>
    </tr>
</table>

<a href="/logout">Logout</a>

<% include('../layouts/footer.ejs') %>